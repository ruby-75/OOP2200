package org.example;
//implements action listeners for the buttons
import java.awt.event.*;

//handles events from all the number buttons
public class NumberBtnHandler implements ActionListener{
    @Override
    /*e is a parameter we name that can access the Objects getSoure()
    method to get the object where an event initially occurred,
    which is useful when checking for the clicked button.
     */
    public void actionPerformed(ActionEvent e){

    }
}
